// Simple JS for future expansion
console.log("JioMart Digital – Mandapeta loaded");

// Example: highlight finance section on load
document.addEventListener("DOMContentLoaded", () => {
  const finance = document.querySelector(".highlight");
  finance.style.transition = "0.4s";
});
